# No Capability Spec Changes

This change contains no new capabilities and no modified spec-level requirements.

The `scrollable-page` spec already defines the refresh contract (pull-to-refresh, wheel-overscroll, spinner lifecycle). This change is purely a wiring fix in the home page consumer — it restores correct behaviour without changing any specified interface or requirement.
