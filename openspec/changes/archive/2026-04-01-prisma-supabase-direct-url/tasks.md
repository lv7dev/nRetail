## 1. Prisma Config

- [x] 1.1 Add `directUrl: process.env['DIRECT_URL']` to datasource block in `prisma.config.ts`

## 2. Environment Template

- [x] 2.1 Add `DIRECT_URL` to `backend/.env.example` with comment explaining pooled vs direct usage
